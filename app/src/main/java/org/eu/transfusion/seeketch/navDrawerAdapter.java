package org.eu.transfusion.seeketch;

import android.support.v7.widget.RecyclerView;
import android.view.View;
import android.widget.ImageView;
import android.widget.TextView;

/**
 * Created by transfusion on 15-9-27.
 */
/*
public class navDrawerAdapter extends RecyclerView.Adapter<navDrawerAdapter.navDrawerViewHolder>{

    public static class navDrawerViewHolder extends RecyclerView.ViewHolder {

        boolean isHeader;
        TextView itemName;
        ImageView itemIcon;
        navDrawerViewHolder(View v, boolean isHeader){
            super(v);
            this.isHeader = isHeader;
            if (isHeader){

            }
            else {
                itemName = (TextView) itemView.findViewById(R.id.rowText);
                itemIcon = (ImageView) itemView.findViewById(R.id.rowIcon);
            }
        }

    }

    navDrawerAdapter(String[] navItems, int[] ){

    }
}*/
